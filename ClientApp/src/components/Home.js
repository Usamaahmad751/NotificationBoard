import React, { Component } from 'react';

class Home extends Component {
  static displayName = Home.name;

  render () {
      return (
          <React.Fragment>
          </React.Fragment>
    );
  }
}
export default Home